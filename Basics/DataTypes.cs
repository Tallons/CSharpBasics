using System;
using System.Collections.Generic;
using System.Text;

namespace Basics {
    class DataTypes {
        static void dataTypes () {

            int a = -5;
            uint b = 5; // cant be negative
            char c = 'c';
            float d = 5.5F; //not used often (save memory, but not as accurate as double)
            double e = 5.5;
            decimal f = 5.5M; // more accurate than doubles - heavy math or using money
            bool g = true;

        }
    }
}
